# DS-043: Show yanked installed versions even when otherwise current

- **Status:** Closed
- **Priority:** P1
- **Scope:** Table and summary reporting / freshness risk

## Evidence

- `crates/depscan-report/src/lib.rs:98-112` renders registry status only when `staleness > Current`.
- The `(installed version yanked)` annotation is nested inside that block, so an installed version equal to latest stable but yanked is hidden.
- Totals at `crates/depscan-report/src/lib.rs:271-279` likewise count only staleness, not yanked/deprecated installed releases.

## Acceptance criteria

- Render a prominent yanked/deprecated-installed warning regardless of staleness.
- Define whether yanked packages affect totals and failure policy, then implement consistently across table, JSON, SARIF, and summary.
- Do not label the latest release itself yanked when only the installed artifact/file is yanked.

## Verification

- Add renderer/process fixtures for yanked-current, yanked-outdated, partially yanked PyPI files, and non-yanked current versions across all formats.

## Resolution

- Yanked/deprecated status is rendered as an independent red `YANKED` table finding even when the installed version sorts as current; an outdated yanked package also retains its separate update line.
- JSON retains the installed-version `latest.yanked` field, SARIF emits a `DEPSCAN-YANKED` warning result, and summaries/totals include a dedicated yanked count.
- A yanked package contributes once to the actionable/outdated total. Any enabled `--fail-on-outdated` threshold treats it as a failure and returns `2`; `never` remains report-only.
- The README documents that `yanked` describes the installed version and that a PyPI release is yanked only when every file is yanked.

## Verification evidence

- Renderer and process fixtures cover yanked-current in table/JSON/SARIF/summary, yanked-outdated without double counting, non-yanked current, and fully versus partially yanked PyPI releases.
- `cargo fmt --all -- --check`
- `cargo test --workspace --all-targets` (81 tests)
- `cargo clippy --all-targets -- -D warnings`
- `cargo +1.95.0 test --workspace --all-targets` (81 tests)
