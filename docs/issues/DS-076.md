# DS-076: Add an auditable Markdown report format

- **Status:** Closed
- **Priority:** P2
- **Scope:** Human-readable Markdown reporting across the CLI and composite GitHub Action

## Evidence

- The CLI supports terminal tables, JSON, SARIF, and a one-line summary, but none of those formats
  is directly suitable for durable pull-request comments, job summaries, or repository audit files.
- The composite action validates report formats independently, so adding only a CLI enum would
  leave action consumers unable to request the new format.
- Package names, advisory summaries, provider diagnostics, and suppression reasons are external
  text and must not be allowed to inject Markdown tables, links, images, or raw HTML.

## Acceptance criteria

- Add an explicit `markdown` output format and infer it from `.md` and `.markdown` output paths
  without changing the existing terminal or redirected-output defaults.
- Render deterministic summary, vulnerability, dependency-status, suppression, and soft-failure
  sections, including unresolved manifest constraints and withdrawn/yanked state.
- Escape every dynamic Markdown table cell, including multiline and raw-HTML-shaped input.
- Accept the format through the composite GitHub Action's typed argument boundary.
- Keep implementation and tests in dedicated modules below the repository's 400-line ceiling.
- Update CLI help, README, issue traceability, and process/report/action regressions.

## Verification

- Run `cargo +1.97.1 test -p depscan-report --lib --locked`.
- Run the focused CLI output-format and report process suites.
- Run `bash scripts/verify-github-action.sh` and `bash scripts/check-rust-architecture.sh`.
- Run `cargo +1.97.1 test --workspace --all-targets --locked`.
- Run `cargo clippy --all-targets -- -D warnings` and `cargo fmt --all -- --check`.

## Resolution and evidence

Implemented an explicit, opt-in Markdown report without changing the existing terminal or
redirected-output defaults. The renderer lives in its own bounded module, reuses the report's
canonical totals and dependency-status decisions, and emits summary, vulnerability,
dependency-status, suppression, and soft-failure sections. Dynamic table cells escape Markdown,
raw HTML, controls, and line breaks. A compact provenance line beneath the title identifies Gedank
Rayze DepScan, the compiled package version, and the canonical GitHub repository when the report is
shared independently. `.md` and `.markdown` infer the format, while the CLI and composite Action
also accept the typed `markdown` value directly.

The additive public report and CLI contract moved the workspace and internal dependency
requirements from `1.1.0` to `1.2.0`. Packaging all five crates through Cargo's temporary registry
proved that the CLI consumes the packaged `depscan-report` API rather than an already-published
older crate.

Verification on 2026-08-20:

- `cargo +1.97.1 test -p depscan-report --lib --locked`: 14 passed.
- `cargo +1.97.1 test -p depscan-cli --test exit_codes --locked`: 69 passed.
- `cargo +1.97.1 test -p depscan-cli --test e2e_matrix --locked
  offline_ecosystem_output_matrix`: one ecosystem/output matrix passed and exercised Markdown for
  npm, PyPI, NuGet, and Cargo fixtures.
- `cargo +1.97.1 test --workspace --all-targets --locked` and
  `cargo +1.95.0 test --workspace --all-targets --locked`: each passed 425 tests with one intentional
  live-provider ignore.
- `cargo clippy --all-targets -- -D warnings`, `cargo +1.97.1 fmt --all -- --check`, Actionlint,
  ShellCheck, the Node syntax check, `scripts/verify-github-action.sh`,
  `scripts/verify-release-workflow.sh`, and `git diff --check`: passed.
- `cargo +1.97.1 package --workspace --allow-dirty --locked`: packaged and verified all five crates.
- `cargo audit`: 308 dependencies scanned against 1,217 advisories with no vulnerability reported.
- `scripts/check-rust-architecture.sh`: 247 Rust files checked, 400-line maximum preserved, tests
  isolated.
- Real local scans wrote and inspected Markdown reports for Python `any-to-markdown` (64 packages,
  2 vulnerabilities), Rust `bp` (116/5), Node `fatsy` (488/78), Bun `tinycrew` (103/12), and .NET
  `demo-rest` (6/0). All exited successfully; the .NET manifest-only alpha constraint remained
  visibly unresolved instead of being presented as current.
