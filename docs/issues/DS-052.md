# DS-052: Degrade unavailable Bun binary-lock extraction to manifests

- **Status:** Closed
- **Priority:** P1
- **Scope:** Bun detection / manifest parsing / optional tool orchestration

## Evidence

- Specification section 3.1 requires a project with `bun.lockb` and no usable Bun executable to degrade to `package.json` manifest constraints and emit a warning.
- Detection selects `BunLockBinary` before `package.json`. Without `allow-tools`, the parser therefore returns a usage error even when a valid manifest is present.
- With `allow-tools`, a missing executable is also treated as an unconditional usage error. Root and workspace manifests are never considered, so the documented manifest-only mode is unavailable for this common legacy Bun layout.

## Acceptance criteria

- Preserve resolved extraction when Bun execution is explicitly authorized and succeeds.
- When tool execution is unauthorized or Bun cannot be prepared or started, parse the colocated root and declared workspace `package.json` files, preserve exact range provenance/direct/development metadata, and emit a visible degraded-mode warning.
- Fail with exit `10` when neither resolved extraction nor a complete, safely contained manifest set is usable.
- Once an explicitly authorized Bun process starts, retain DS-023's hard failures for non-zero status, timeout, oversized/non-UTF-8 output, capture errors, and malformed lock output.
- Never start Bun without the effective `allow-tools` authorization.

## Verification

- Parser tests cover root/workspace production and development constraints, non-registry workspace references, malformed manifests, missing manifests, and escaping workspace patterns.
- Process tests cover unauthorized no-start degradation, missing executable degradation, exact provenance in JSON output, and fail-closed missing/malformed manifest behavior.
- Run locked workspace/all-target tests on Rust 1.97.1 and 1.95.0, exact strict Clippy, formatting, and diff checks.

## Resolution

- Binary Bun detection now has an explicit three-way boundary. Successful authorized extraction keeps resolved lockfile coordinates; disabled execution or a pre-start `Missing`/`Setup`/`Spawn` failure enters the manifest-only path; every failure after a process starts remains a usage/parse error.
- The manifest fallback requires a colocated root `package.json`, validates its JSON shape and dependency-section types, expands array and object-form workspace declarations only through contained relative patterns, validates every matched manifest inside the project root, and applies pattern/manifest-count bounds. Missing, malformed, escaping, or incomplete workspace inputs fail with source-qualified exit `10` diagnostics.
- Root and workspace production, development, optional, and peer dependencies retain exact `ManifestConstraint` values, declaring source paths, direct/development classifications, and `resolved_from_range=true`. Workspace/file/link/VCS/URL/alias descriptors remain visible but are not misrepresented as public npm coordinates.
- Successful degradation emits a warning on stderr naming the lockfile and whether authorization or pre-start tool availability caused the fallback. No unauthorized Bun process starts. CLI help and the README now distinguish this path from DS-023's unchanged post-start hard failures.

## Verification evidence

- Rust 1.97.1: `cargo +1.97.1 test --workspace --all-targets --locked` passed 313 tests; the opt-in live-network matrix remained intentionally ignored.
- Rust 1.95.0: `cargo +1.95.0 test --workspace --all-targets --locked` passed the same 313 tests with the same intentional ignore.
- After rebasing onto DS-050, its three online/offline manifest-to-OSV process tests passed alongside all 11 external-tool process tests and both Bun manifest-fallback parser tests.
- `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all -- --check`, and `git diff --check` passed.
