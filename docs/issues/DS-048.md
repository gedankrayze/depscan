# DS-048: Make configuration and report file access capability-safe

- **Status:** In progress
- **Priority:** P1
- **Scope:** CLI configuration / report publication / filesystem integrity

## Evidence

- `crates/depscan-cli/src/main.rs` inspects a configuration path with `symlink_metadata` and later reopens the same pathname with `read_to_string`.
- Implicit configured output containment and symlink checks run before provider work, while the final report is written later through the original pathname.
- A concurrent parent or final-component swap between validation and use can therefore make the process read or overwrite a different file than the one it validated.

## Acceptance criteria

- Read configuration from the same no-follow file handle whose type and location were validated.
- Publish output selected by an implicit project config relative to a capability rooted at the validated scan directory, without following swapped parent or final symlinks.
- Preserve documented CLI and explicitly trusted configuration path semantics while making replacement atomic and failure-safe.
- Preserve the existing Unix mode and portable read-only state when replacing an existing report.
- Return exit `10` for detected component changes; publication and cleanup must remain confined to held capabilities and never follow a substituted final symlink, including in the non-atomic interval between the last identity check and rename.

## Resolution

- The CLI opens the scan root once. The same `cap_std::fs::Dir` capability is used to read an implicit `depscan.toml` and is moved into the configured report publisher, closing the former config-to-output handoff race.
- Implicit and explicit configuration files are opened as regular files without following their final component, read from that validated handle, and checked again against their containing capability and file identity.
- Implicit configured reports traverse parent directories with no-follow capability operations, retain the root and parent handles for the complete scan, and publish with a same-directory `cap_tempfile::TempFile`.
- Before publication, the temporary report is flushed, synced, assigned the existing target's portable permissions, and the root, parent, and target identities are rechecked. Replacement is atomic within the held parent directory.
- A portable file API cannot combine inode comparison and rename into one compare-and-swap. A final-component change after the last check may therefore be replaced, but it cannot redirect the operation: rename replaces that entry relative to the held parent and does not follow a substituted symlink. CLI and explicitly trusted config outputs intentionally retain their prior ambient-path behavior.

## Verification

- Deterministic barrier tests replace the configuration file, output file, and output parent between validation and use. Additional tests cover the config-to-output root handoff, final and parent symlinks, missing targets, atomic replacement, and Unix `0600` preservation.
- Windows-specific tests cover the held-directory behavior and both symlink-privileged and privilege-denied runners. The repository's quality matrix runs the complete suite on Ubuntu, macOS, and Windows; this local verification executed on macOS, so native Linux and Windows runs remain observable in CI rather than being claimed here. The issue therefore remains In progress.
- Rust 1.97.1: `cargo fmt --all -- --check`, all 257 workspace/all-target tests, and exact `cargo clippy --all-targets -- -D warnings` passed.
- Rust 1.95.0: all 257 locked workspace/all-target tests passed.
- A fresh `cargo audit --file Cargo.lock --deny warnings` loaded 1,217 RustSec advisories and reported no vulnerabilities across 306 locked dependencies.
- An independent read-only review reran 69 CLI tests on both toolchains plus exact Clippy and found no remaining release-blocking issue.
