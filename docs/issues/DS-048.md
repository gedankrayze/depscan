# DS-048: Make configuration and report file access capability-safe

- **Status:** Open
- **Priority:** P1
- **Scope:** CLI configuration / report publication / filesystem integrity

## Evidence

- `crates/depscan-cli/src/main.rs` inspects a configuration path with `symlink_metadata` and later reopens the same pathname with `read_to_string`.
- Implicit configured output containment and symlink checks run before provider work, while the final report is written later through the original pathname.
- A concurrent parent or final-component swap between validation and use can therefore make the process read or overwrite a different file than the one it validated.

## Acceptance criteria

- Read configuration from the same no-follow file handle whose type and location were validated.
- Publish output selected by an implicit project config relative to a capability rooted at the validated scan directory, without following swapped parent or final symlinks.
- Preserve documented CLI and explicitly trusted configuration path semantics while making replacement atomic and failure-safe.
- Return exit `10` without reading or modifying an out-of-scope target when any component changes during the operation.

## Verification

- Add deterministic barrier-based tests that swap the config file, output file, and output parent after validation but before read/publication.
- Cover regular files, final symlinks, parent symlinks, missing targets, and atomic replacement on every supported OS.
- Run the Rust 1.97.1 workspace tests and exact `cargo clippy --all-targets -- -D warnings`, plus the locked Rust 1.95.0 suite.
