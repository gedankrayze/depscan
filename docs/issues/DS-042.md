# DS-042: Make JSON output reproducible when requested

- **Status:** Closed
- **Priority:** P1
- **Scope:** Core scan document / JSON contract

## Evidence

- `crates/depscan-core/src/lib.rs:246-258` assigns `generated_at = Utc::now()` inside `ScanDocument::new`.
- Identical scans therefore always produce byte-different JSON, complicating snapshots, caching, diff-based CI, and reproducible evidence.
- The clock is not injectable, so tests cannot control the value without post-processing.

## Acceptance criteria

- Inject scan time from orchestration and define a deterministic/reproducible mode (for example `SOURCE_DATE_EPOCH` or an explicit output option) while retaining useful timestamps by default.
- Sort all JSON collections whose order is not semantically meaningful.
- Document timestamp/timezone/schema semantics and preserve backward compatibility intentionally.

## Verification

- Run identical fixture scans twice in reproducible mode and assert byte-identical JSON.
- Add controlled-clock serialization tests and verify normal mode emits one valid UTC timestamp per scan.

## Resolution

- Scan orchestration now supplies the document timestamp through `ScanDocument::at`; `ScanDocument::new` retains the real current UTC time for normal callers.
- Setting `SOURCE_DATE_EPOCH` to integer Unix seconds selects reproducible mode. Invalid UTF-8, non-integer, and out-of-range values are usage/configuration errors (`10`) before provider access.
- Document construction canonicalizes package results plus vulnerability, alias, fixed-version, reference, enrichment-error, and suppression collections. Fixed versions retain ecosystem-aware version ordering.
- The README documents RFC 3339 UTC semantics, the default clock, and a reproducible CI invocation.

## Verification evidence

- Two process-level fixture scans with `SOURCE_DATE_EPOCH=1700000000` produced byte-identical JSON with `generated_at` equal to `2023-11-14T22:13:20Z`.
- Controlled-clock and current-UTC core tests passed.
- `cargo fmt --all -- --check`
- `cargo test --workspace --all-targets` (69 tests)
- `cargo clippy --all-targets -- -D warnings`
- `cargo +1.95.0 test --workspace --all-targets` (69 tests)
