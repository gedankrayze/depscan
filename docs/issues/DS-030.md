# DS-030: Enforce offline dump age and use cached registry metadata

- **Status:** Closed
- **Priority:** P1
- **Scope:** Offline mode / cache policy

## Evidence

- Sync writes `*.synced-at` at `crates/depscan-providers/src/lib.rs:650-656`, but `OsvOffline::query_blocking` at lines `526-613` never reads or validates it.
- `crates/depscan-cli/src/main.rs:225-250` constructs a cache policy, then unconditionally replaces all offline freshness results with an empty map instead of reading usable cached registry entries.
- `--max-cache-age` therefore does not guard OSV dump age, and offline scans cannot return registry freshness even when cached metadata exists.

## Acceptance criteria

- Parse and validate each dump's age; warn above the default seven-day age and enforce the documented `--max-cache-age` behavior.
- Missing, future, or malformed timestamps are handled visibly and safely.
- Offline registry lookup reads cache only, performs no network requests, respects maximum age, and reports stale/missing entries as `Unknown` with an explicit reason.
- Offline mode has a network-deny integration test.

## Verification

- Test fresh, stale, missing, malformed, and future dump markers with a controlled clock.
- Preseed fresh/stale registry cache entries and assert offline results and warnings without any mock-server request.

## Resolution

- `OsvOffline` now reads the `*.synced-at` marker paired with every required ecosystem archive against one captured UTC time. Missing, malformed, future-dated, symlinked, and non-file markers fail visibly and direct the operator to `depscan sync`; the offline directory is also required to be a real directory inside the selected cache.
- With no explicit age limit, an archive older than seven days remains usable but emits a structured warning. `--max-cache-age` replaces that warning-only default with a hard maximum: an older dump is rejected before any advisory entry is evaluated.
- `RegistryOffline` has no HTTP client and reads only the existing content-addressed registry cache. It reuses the same npm, PyPI, NuGet, and crates.io response validation and version-selection functions as the online provider. The normal six-hour registry TTL applies by default, while an explicit `--max-cache-age` selects the older-data tolerance for an air-gapped scan.
- Missing, stale, future-dated, corrupt, invalid, symlinked, or `--no-cache` registry entries become `LatestVersions::unknown()` plus a per-package `registry` error that states the exact cache reason. A bad registry entry is therefore visible without turning an otherwise valid offline vulnerability scan into a provider-wide failure.
- Offline orchestration no longer constructs `HttpClient`. A process test poisons every proxy setting and still obtains a concrete latest version from the fresh cache; missing and stale cases produce `Unknown` with cache-specific reasons rather than a network error.

## Verification evidence

- `cargo +1.97.1 test --workspace --all-targets --no-fail-fast`: 228 tests passed.
- `cargo +1.97.1 clippy --all-targets -- -D warnings`: passed.
- `cargo +1.95.0 test --workspace --all-targets --locked --no-fail-fast`: 228 tests passed at the declared MSRV.
- Deterministic provider tests cover fresh, warning-age, explicitly stale, missing, malformed, and future dump timestamps; fresh/tolerated, stale, missing, corrupt, future, and cache-disabled registry states; and valid cached metadata for all four ecosystems.
- Process coverage proves a fresh offline registry result with all network proxies denied, stale/missing `Unknown` reports with explicit reasons, the seven-day warning, and hard `--max-cache-age` rejection. `Cargo.lock` is unchanged.
